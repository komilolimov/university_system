import { LoginCard } from "@/widgets/auth";

export default function LoginPage() {
  return (
    <main className="min-h-screen bg-transparent flex flex-col font-sans">
      <LoginCard />
    </main>
  );
}


